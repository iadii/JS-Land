const allLinks = document.querySelectorAll('a')

for(let links of allLinks){
    links.style.color = 'rgb(0, 108, 134)';
    links.style.textDecorationStyle = 'none';
    links.style.fontSize = 'larger'
}